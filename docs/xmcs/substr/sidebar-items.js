initSidebarItems({"struct":[["SubString","Given two strings `s1` and `s2` and an integer `delta`, this struct holds data used to answer the following question in constant time: for any `i`, `j` indices of `s1`, `s2` such that the distance between `i` and `j` is less than `delta` is `s1[i..]` is a substring of `s2[j..]` or `s2[j..]` a substring of `s1[i..]`, It only needs to do a precomputation in time `O(|s1| * delta)` beforehand."]]});